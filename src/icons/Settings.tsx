import { Settings } from 'lucide-react'

export default function SettingsIcon() {
  return <Settings size={28} strokeWidth={1.75} />
}

export function SettingsIconLight() {
  return <Settings size={20} strokeWidth={1.75} />
}
