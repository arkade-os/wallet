import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.arkade.wallet',
  appName: 'Arkade Wallet',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config
