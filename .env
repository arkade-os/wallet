CI=false
GENERATE_SOURCEMAP=false

# On for this branch so preview deploys exercise the activity path.
# Unset or set to anything else to fall back to the flat tx history.
VITE_USE_SDK_ACTIVITY_HISTORY=true
