# Vendored packages

`arkade-os-swap-0.0.14-pr899-fa662d0.tgz` was built with `pnpm pack` from
[`arkade-os/ts-sdk#899`](https://github.com/arkade-os/ts-sdk/pull/899) commit
`fa662d0f`. The tarball pins the exact swap compatibility implementation while
that pull request is under review; replace it with a published package release
after the upstream change lands.
